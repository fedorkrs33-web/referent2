# Referent — Парсер статей с AI

Приложение получает URL англоязычной статьи, парсит её и с помощью ИИ генерирует:
- Краткое описание
- Тезисы
- Пост для Telegram
- Иллюстрации к статье

## 💡 Интерфейс

- Поле ввода URL
- Кнопки: «О чем статья?», «Тезисы», «Пост для Telegram», «Иллюстрация»
- Блок вывода результата

## 🚀 Запуск

```bash
npm install
npm run dev
```

## ⚙️ Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
# GigaChat API (для текстовых запросов)
GIGACHAT_CLIENT_ID=your_gigachat_client_id
GIGACHAT_CLIENT_SECRET=your_gigachat_secret
GIGACHAT_RQ_UID=your_rq_uid

# Генератор изображений
# Возможные значения: polza, dalle, openai
# По умолчанию: polza
IMAGE_GENERATOR=polza

# Polza.ai API (для IMAGE_GENERATOR=polza)
POLZA_API_KEY=your_polza_api_key

# OpenAI API (для IMAGE_GENERATOR=dalle или IMAGE_GENERATOR=openai)
OPENAI_API_KEY=your_openai_api_key
```

### Генераторы изображений

Проект поддерживает несколько генераторов изображений:

1. **Polza.ai** (по умолчанию)
   - Переменная: `IMAGE_GENERATOR=polza`
   - Требует: `POLZA_API_KEY`
   - Поддерживает получение по requestId

2. **OpenAI DALL-E 3**
   - Переменная: `IMAGE_GENERATOR=dalle` или `IMAGE_GENERATOR=openai`
   - Требует: `OPENAI_API_KEY`
   - Быстрая генерация, высокое качество

## 📚 Дополнительно

Подробное описание проекта: `PROJECT.md`
